id: clean-production-db-backups
namespace: prod.aws

description: |
  Retention for the date-stamped MySQL backups in s3://bondlink-data-east. backups/mysql/bondlink-us-east-1
  gets GFS bands -- every entry kept for 366 days, Sundays only for the year before that,
  first-of-month only back to 20 years. Storage class and any flat age-based expiry belong to the
  bucket's lifecycle rules; this job exists only for what lifecycle cannot express.

  Bucket, prefix, retention policy and delete mode are all required arguments. Delete mode is a
  property of the (bucket, prefix) pair rather than the bucket: marker mode leaves a delete marker
  for lifecycle to reap and is refused unless an enabled rule covers the prefix with both
  NoncurrentVersionExpiration and ExpiredObjectDeleteMarker. Deletes are opt-in -- the script
  reports and exits unless passed --live.

  backups/mysql/mariadb-dump follows the same policy but has no reaping rule on this bucket, so the
  script refuses it; it joins this flow once that lifecycle rule exists.

  aws/clean_database_backups.sh is a Namespace File
  (namespace-files/prod.aws/clean_database_backups.sh in this repo, synced to
  Kestra's own prod.aws namespace), pulled in via
  {{ read('clean_database_backups.sh') }} (no namespace= override needed —
  read() defaults to the calling flow's own namespace) rather than being
  inlined directly. This isn't about sharing across flows — it's the fix for a
  real production failure (see clean-corp-preview-s3.yml, which hit the exact
  same bug): the script's `${#deleteKeys[@]}`-style bash array-length syntax
  contains a literal `{#`, which Kestra's Pebble template engine parses as the
  start of a *Pebble* comment (`{# ... #}`); since bash never emits a matching
  `#}`, Pebble scans to end-of-file still inside the "comment" and throws
  `ParserException: Unclosed comment` at execution time. Escaping every
  `${#...}` occurrence is fragile and easy to reintroduce; read()'s return
  value is never re-parsed for template syntax, so this sidesteps the whole
  class of bug.

tasks:
  - id: clean_db_backups
    type: io.kestra.plugin.fs.ssh.Command
    host: prodsalt.bondlink.vpc
    port: "2007"
    username: bldeploy
    authMethod: PUBLIC_KEY
    privateKey: "{{ secret('SSH_PRIVATE_KEY') }}"
    strictHostKeyChecking: "no"
    commands:
      - |
        set -e
        cat > /tmp/clean_database_backups.sh <<'CLEAN_DB_BACKUPS_SCRIPT'
        {{ read('clean_database_backups.sh') }}
        CLEAN_DB_BACKUPS_SCRIPT
        chmod +x /tmp/clean_database_backups.sh
        /tmp/clean_database_backups.sh --bucket bondlink-data-east --prefix backups/mysql/bondlink-us-east-1 --retain banded --mode marker --live
        rm -f /tmp/clean_database_backups.sh

triggers:
  - id: schedule
    type: io.kestra.plugin.core.trigger.Schedule
    cron: "0 10 * * *"
    timezone: UTC

errors:
  - id: notify_failure
    type: io.kestra.plugin.notifications.sendgrid.SendGridMailSend
    sendgridApiKey: "{{ secret('SENDGRID_API_KEY') }}"
    from: kestra@bondlink.com
    to:
      - jeff@bondlink.com
    subject: "Kestra[prod_cron] Clean Production DB backups failed"
    htmlContent: |
      Execution {{ execution.id }} of {{ flow.namespace }}.{{ flow.id }} failed.
      Failed task: {{ tasksWithState('FAILED')[0].id }}
