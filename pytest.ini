[pytest]
testpaths = tests
markers =
    integration: opt-in tests that make real AWS API calls (needs real credentials, not run by default - see `make test-integration`)
addopts = -m "not integration"
